# Domain-decomposition results

`current-pin/campaign.json` appears only after the external runner records at
least one serialized GPU case. The notebook prefers that manifest over the
`NOT REPORTED` template, but plots it only after all 1/2/4-GPU provenance,
checksum, ownership, and parity checks pass.

No archived measurement is copied into this directory.
