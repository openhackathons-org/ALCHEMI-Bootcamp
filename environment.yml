name: alchemi-bmd-materials
channels:
  - https://conda.ovito.org
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - numpy>=1.24
  - matplotlib>=3.7
  - pandas>=2.0
  - jupyter
  - ipykernel
  - ovito=3.15.0
  - pip
  - pip:
      - ase>=3.22
      - pymatgen>=2024.1.1
      - pydantic>=2.0
      - requests>=2.31
      - rdkit>=2023.03
      # Dev dependencies
      - pytest>=8.0
      - ruff>=0.4
