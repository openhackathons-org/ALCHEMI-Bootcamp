<!-- Shared implementation templates. The authoring guide decides when to use them. -->

## Highlight

> 💡 One durable mental model with an explicit inline API name such as `Batch`.

## API

> <span aria-label="ALCHEMI Toolkit API" style="color:currentColor;color:color-mix(in srgb,currentColor 68%,#76B900 32%);font-size:0.70rem;font-weight:700;letter-spacing:0.05em;">&lt;/&gt;&nbsp; API</span>
>
> `public_api(argument)`
>
> **Input:** the input form that matters here
>
> **Result:** the result the learner will inspect

Use a quoted Python fence when the call needs several lines:

> <span aria-label="ALCHEMI Toolkit API" style="color:currentColor;color:color-mix(in srgb,currentColor 68%,#76B900 32%);font-size:0.70rem;font-weight:700;letter-spacing:0.05em;">&lt;/&gt;&nbsp; API</span>
>
> ```python
> result = public_api(
>     argument,
>     option=value,
> )
> ```
>
> **Input:** the input form that matters here
>
> **Result:** the result the learner will inspect
