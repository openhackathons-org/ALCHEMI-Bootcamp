"""Internal support code for the scalable atomistic workflow tutorial.

Notebook cells should import focused modules (for example, ``aux.spectra``)
rather than relying on a broad package-level helper surface.
"""

__all__: tuple[str, ...] = ()
