# Runtime and tooling dependencies for the remastered Part 1 image.
#
# ovito is installed via conda (see build/environment.yml) so its bundled
# Qt6 / glib / fontconfig runtime ships with the conda env instead of via
# apt-get system libs. See build/Dockerfile and the repository README for
# the full install sequence.

# Toolkit packages pinned to exact source commits for a reproducible
# environment. Runtime validation is recorded separately. The Dockerfile
# installs with `--no-sources-package nvalchemi-toolkit-ops` so Toolkit's
# internal `tool.uv.sources` revision cannot override the exact pin below.
torch==2.12.0
nvalchemi-toolkit-ops[torch-cu13,jax-cu13] @ git+https://github.com/NVIDIA/nvalchemi-toolkit-ops.git@e8e7a7464f6745277a156a3d6f433d06b58c60e3
# Match the JAX version resolved and tested by the pinned Toolkit-Ops commit.
jax[cuda13]==0.9.0.1

# Toolkit core used throughout the playbook; pulls in ASE transitively.
nvalchemi-toolkit[ase,aimnet] @ git+https://github.com/NVIDIA/nvalchemi-toolkit.git@331d6b2a17d7aabe64a3c77bc9b0cfdbc0e85409

# Explicit model/runtime pins used by the remastered notebooks.
aimnet==0.2.0
sevenn==0.13.0
# SevenNet requires e3nn >=0.5 because that release changed the
# Clebsch-Gordan coefficient convention. Pin the tested final 0.5 release.
e3nn==0.5.9
nvidia-physicsnemo==2.1.1
warp-lang==1.13.0

# Scientific and notebook stack
pymatgen>=2024.1.1
pydantic>=2.0
requests>=2.31
aiohttp>=3.9
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Jupyter / interactive
ipykernel
ipywidgets>=8.0
jupyterlab
jupyterlab-vim

# Retained trajectory MP4 support. imageio_ffmpeg ships a static ffmpeg binary,
# so no apt-get ffmpeg is needed in the image.
imageio-ffmpeg

# Test + lint tooling (formerly requirements-dev.txt; included unconditionally
# so the image always ships the same toolchain regardless of build target).
pytest>=8.0
ruff>=0.4
