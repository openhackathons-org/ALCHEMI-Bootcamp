# Unified runtime + tooling dependencies for the ALCHEMI Playbook (Parts 1–3).
#
# ovito is installed via conda (see dev/environment.yml) so its bundled
# Qt6 / glib / fontconfig runtime ships with the conda env instead of via
# apt-get system libs. See dev/Dockerfile and dev/README.md for the
# full install sequence.

# Toolkit packages pinned to validated source commits for a reproducible
# environment. The Dockerfile installs with `--no-sources-package
# nvalchemi-toolkit-ops` so Toolkit's internal `tool.uv.sources` revision
# cannot override the directly validated commit below.
torch==2.12.0
nvalchemi-toolkit-ops[torch-cu13] @ git+https://github.com/NVIDIA/nvalchemi-toolkit-ops.git@c6fbe652315e0cebd4f57a6a25f626258f0dbbfd

# Toolkit (Part 3 core; pulls in ase as a transitive)
nvalchemi-toolkit[ase,mace,aimnet] @ git+https://github.com/NVIDIA/nvalchemi-toolkit.git@b770ee6963fd2f6137891e408c370012751918e2

# Explicit transitive pins used by the remastered Toolkit-foundations notebook.
aimnet==0.2.0
nvidia-physicsnemo==2.1.1
warp-lang==1.13.0

# cuEquivariance compiled Torch ops for the CUDA 13 runtime. This matches the
# cuequivariance/cuequivariance-torch 0.10.0 stack pulled by mace-torch and
# prevents the MACE path from falling back to the naive implementation.
cuequivariance-ops-torch-cu13==0.10.0

# ORB-v3 foundation model used by Part 2. Its 0.7.0 metadata still caps
# Toolkit-Ops below 0.4; build/overrides.txt keeps the playbook's validated
# Toolkit-Ops source pin authoritative during unified resolution.
orb-models==0.7.0
loguru

# Parts 1 and 2 scientific stack
pymatgen>=2024.1.1
pydantic>=2.0
requests>=2.31
aiohttp>=3.9
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# Jupyter / interactive
ipykernel
ipywidgets>=8.0
jupyterlab
jupyterlab-vim

# Part 2 packing
packmol

# Parts 1 and 2 trajectory MP4 rendering (imageio_ffmpeg ships a static ffmpeg binary,
# so no apt-get ffmpeg is needed in the image).
imageio-ffmpeg

# Test + lint tooling (formerly requirements-dev.txt; included unconditionally
# so the image always ships the same toolchain regardless of build target).
pytest>=8.0
ruff>=0.4
