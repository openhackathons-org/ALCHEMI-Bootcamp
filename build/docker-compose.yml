services:
  jupyter:
    build:
      context: ..
      dockerfile: build/Dockerfile
    image: alchemi-playbook:latest
    container_name: alchemi-playbook-jupyter
    # OVITO ships only x86_64 builds on conda.ovito.org; the container
    # must therefore run on an x86_64 host. See ../README.md.
    platform: linux/amd64
    networks: [alchemi-net]
    ports:
      - "8888:8888"
    shm_size: 8g
    # Selective bind-mounts. Students see only tutorial content under
    # /workspace; build/, start-*, and dotfiles stay hidden.
    volumes:
      - ../part-2-batched-adsorption-toolkit:/workspace/part-2-batched-adsorption-toolkit
      - ../part-1-scalable-atomistic-workflows:/workspace/part-1-scalable-atomistic-workflows
      - ../part-3-batched-melting-toolkit:/workspace/part-3-batched-melting-toolkit
      - ../research-toolkit-foundations:/workspace/research-toolkit-foundations
      - ../README.md:/workspace/README.md:ro
      - ../LICENSE:/workspace/LICENSE:ro
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

networks:
  alchemi-net:
    driver: bridge
